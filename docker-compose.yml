services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: breakpoint
      POSTGRES_PASSWORD: breakpoint
      POSTGRES_DB: breakpoint
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
